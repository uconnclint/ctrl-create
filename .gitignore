# OS / editor
.DS_Store
*.swp

# local agent / dev config (machine-specific paths)
.claude/

# throwaway art-generation working files (high-res sources; not shipped)
tmp/

# node (none committed, but just in case)
node_modules/

# Cloudflare Workers dev/deploy cache (account id + local state, not secrets,
# but not something a fresh clone needs) — Q6 Pages -> Workers deploy config.
.wrangler/
